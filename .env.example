# Copy to .env.local and fill in. Never commit real secrets.

# --- Admin panel (/admin) ---
# Password staff type to sign in to /admin.
ADMIN_PASSWORD=change-me-strong-password
# Opaque session token stored in the auth cookie. Use a long random string.
ADMIN_TOKEN=change-me-to-a-long-random-string

# --- Lead forms (wire up for go-live; see REQUIREMENTS.md §6) ---
# RESEND_API_KEY=
# LEADS_TO_EMAIL=info@strucon.net
# CRM_WEBHOOK_URL=
