# dependencies / build output
/node_modules
/.next
/out
/build
*.tsbuildinfo
next-env.d.ts

# environment — never commit real secrets
.env
.env.local
.env*.local
.env.production

# editor / OS
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# hosting
.vercel

# NOTE: content-data/ IS committed on purpose — it holds live content saved from
# the /admin panel, so the deployed site ships the content staff actually published.
